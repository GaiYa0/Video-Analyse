# 当前阶段

```yaml
phase: 2
```

协作 AI **先读本文件再写业务代码**。阶段禁令只写在这里。过验收后只改上面的 `phase`，不要去改角色手册。

升级方式：验收点 N 通过后，把 `phase` 改成 **N+1**（点 4 通过后保持 `4`），并在文末记一行。由同学 C 开 `docs/` PR，或当场让 AI 只改本文件。

---

## phase = 1（已完成，2026-09-01 关账）

原系统部署、RTSP/直连预览、原 YOLO 布控告警、架构总图、B 的分析器说明（#3）、A 的一键启动与局域网 8080（#4）。不再作为禁令。

---

## phase = 2（当前：验收点 1 已过，做睡岗）

**允许**

- 睡岗：YOLO-Pose 原型、ONNX、接入 `server/`、多帧时序判定
- 布控增加睡岗选项；告警类型与告警页展示睡岗
- 继续修部署、文档；可修 P1 已知的布控详情 `live-output` 404（不要借机改国标）

**禁止**

- GB28181 业务（SIP 5060、ZLM 国标同步、设备 `device_type` / 国标列表页）
- 拆掉或替换原 YOLO；睡岗与国标不要塞进同一个 PR

---

## phase = 3（验收点 2 已过，做国标）

**允许**：ZLM SIP 5060、国标注册/预览/离线、ZLM 同步接口、设备表与列表区分 RTSP/GB28181、Analyzer 取国标播放 URL。

**禁止**：为赶进度拆掉睡岗或原 YOLO；国标与睡岗大改不要塞进同一个 PR。

---

## phase = 4（验收点 3 已过，联调交付）

**允许**：两种设备源的睡岗联调、断线重连、回归原功能、手册 / 视频 / PPT。

**禁止**：无关重构、重写技术栈。

---

## 变更记录

| 日期 | phase | 说明 |
| --- | --- | --- |
| 2026-08-31 | 1 | 初始：验收点 1 |
| 2026-08-31 | 2 | P1 已过，开放睡岗；国标仍禁止 |
| 2026-09-01 | 2 | P1 关账：合入 #3（B 分析器说明）、#4（A 启动脚本）；phase 仍为 2，做睡岗 |
