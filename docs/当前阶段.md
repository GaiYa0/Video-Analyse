# 当前阶段

```yaml
phase: 3
```

协作 AI **先读本文件再写业务代码**。阶段禁令只写在这里。过验收后只改上面的 `phase`，不要去改角色手册。

升级方式：验收点 N 通过后，把 `phase` 改成 **N+1**（点 4 通过后保持 `4`），并在文末记一行。由同学 C 开 `docs/` PR，或当场让 AI 只改本文件。

---

## phase = 1（已完成，2026-09-01 关账）

原系统部署、RTSP/直连预览、原 YOLO 布控告警、架构总图、B 的分析器说明（#3）、A 的一键启动与局域网 8080（#4）。不再作为禁令。

---

## phase = 2（已完成，2026-09-02 关账）

**允许**

- 睡岗：YOLO-Pose 原型、ONNX、接入 `server/`、多帧时序判定
- 布控增加睡岗选项；告警类型与告警页展示睡岗
- 继续修部署、文档；可修 P1 已知的布控详情 `live-output` 404（不要借机改国标）

**禁止**

- GB28181 业务（SIP 5060、ZLM 国标同步、设备 `device_type` / 国标列表页）
- 拆掉或替换原 YOLO；睡岗与国标不要塞进同一个 PR

---

## phase = 3（当前：验收点 2 已过，做国标）

**允许**：ZLM SIP 5060、国标注册/预览/离线、ZLM 同步接口、设备表与列表区分 RTSP/GB28181、Analyzer 取国标播放 URL。

**禁止**：为赶进度拆掉睡岗或原 YOLO；国标与睡岗大改不要塞进同一个 PR。

操作说明：[国标功能使用说明书.md](./国标功能使用说明书.md)；开机命令：[启动手册.md](./启动手册.md)。

### P3 进度（2026-09-05）

**已完成（同学 A 演示机 · 模拟器主路径）**

- 真 SIP：`scripts/gb28181_sim.py` + `scripts/start_gb_sim.sh` → REGISTER/保活/目录；WVP 设备 `34020000001320000001` 在线（Contact `:15060`，不是 SQL 假在线）。
- 点播出画面：ffmpeg `-f vob` MPEG-2 PS → RTP；ZLM `app=rtp`，stream `34020000001320000001_34020000001320000001`。竖屏片源 pad 成 1280×720。Catalog / DeviceInfo 在 200 OK 里带 XML。
- 业务库只保留一条国标：**demo-ipc**（`ape_id=camgbf0b09a04`）。同步已过滤 SSRC hex / `设备_通道` 重复行 / 半程 `gbcam001`。
- 预览走 Nginx `/rtp/`（与 `/live/` 一样反代 ZLM），本机 `ws://127.0.0.1:8080/rtp/<stream>.live.flv`，不要直连 `:9992`。给 B/C：`rewrite_play_url_for_lan.sh <LAN>`。`zlm_server.host` 保持 `127.0.0.1`。
- 点播联动：预览 / 布控 `warmRtp` 后台 INVITE，不堵 HTTP；WVP `on_stream_not_found` 自动点播；play 前 stop，避免和 WVP 播放器互踩。
- Analyzer 拉国标：`device_type=gb28181` 时从 `play_url` 解析 `/rtp/<设备_通道>`，发给 Analyzer 的是 `rtsp://zlm:9994/rtp/...`；`stimeout` 25s、探测窗口加大。停止监控不清空国标 `play_url`。
- 离线：模拟器 SIGTERM 发 `REGISTER Expires=0`；同步 `is_online` 跟 WVP SIP，不把「ZLM 还有 rtp」当成在线。
- DHCP：模拟器 SDP 强制 `127.0.0.1`；`start_wvp.sh` 把过期的非 loopback `sdp_ip` 改成当前 LAN；WVP 本机播放用 `media.stream-ip=127.0.0.1`（SDP 仍宣局域网 IP）。
- 无人观看：ZLM `streamNoneReaderDelayMS=60000`。
- 预览冻帧已修：SIP Via/`To` tag 解析；前端去掉 live `loop`、修正 flv.js 配置并加 stall 自愈；模拟器 RTP 用单调时钟；WSL 勿开 `systemd-timesyncd`（与宿主机抢时钟会回拨）。
- 现网 WVP：`http://127.0.0.1:18080/` 账号 `admin` / `SvaDemo@2026`；SIP 只绑局域网 IP:5060；平台 `34020000002000000001`；域 `3402000000`；设备密码 `12345678`。
- 文档：`docs/启动手册.md` §2.5–2.7；`docs/国标功能使用说明书.md`。
- P3 验收截图：`docs/photo/p3国标在线状态.png`、`国标同步显示.png`、`国标同步预览视频.png`、`国标设备预览视频（本地的模拟）.png`。

**已知、不影响关账主路径**

- 水杯**模拟器**长推时约每 ~60s 可能闪断几秒，前端 watchdog / 重新 INVITE 会自愈；**真机 IPC 不受此限**。演示、同步、布控、离线主功能可用。

**未完成（关 P3 / 进 phase 4 还差）**

- 真机 IPC 接入（说明书 §8 / 手册 §2.7；先 `pkill -f gb28181_sim.py`）。有真机后再把 `phase` 提到 4。
- 同学 B：国标源睡岗/YOLO 布控联调与回归。
- 同学 C：文档合入核对、联调交付材料（手册/视频/PPT 按 phase 4）。

**下次开机**

1. `.\scripts\start_easysva.ps1`
2. `wsl -d Ubuntu-22.04 -u root -- bash /mnt/e/video-analysis/Video-Analyse-main/scripts/start_wvp.sh`
3. 继续水杯演示：`start_gb_sim.sh`；或接真机见说明书 §8。
4. 网页用 `http://localhost:8080/`（Clash 时不要用局域网 IP）；预览若仍旧画面请 **Ctrl+Shift+R**。

**不要**把 `phase` 改成 4，直到真机注册 + 画面（若验收要求）+ B/C 联调就绪。同学 A 模拟器主路径与截图已齐。

---

## phase = 4（验收点 3 已过，联调交付）

**允许**：两种设备源的睡岗联调、断线重连、回归原功能、手册 / 视频 / PPT。

**禁止**：无关重构、重写技术栈。

---

## 变更记录

| 日期 | phase | 说明 |
| --- | --- | --- |
| 2026-08-31 | 1 | 初始：验收点 1 |
| 2026-08-31 | 2 | P1 已过，开放睡岗；国标仍禁止 |
| 2026-09-01 | 2 | P1 关账：合入 #3（B 分析器说明）、#4（A 启动脚本）；phase 仍为 2，做睡岗 |
| 2026-09-02 | 3 | P2 关账：演示机睡岗告警 + 原 YOLO 进区仍可用（截图 `docs/photo/p2*`）；开放国标 |
| 2026-09-04 | 3 | P3 未关账：模拟器真 REGISTER + WVP 点播有 cup 画面；业务只留 demo-ipc；真机/离线/Analyzer 国标 URL 未做 |
| 2026-09-05 | 3 | 模拟器持续推 PS；Nginx `/rtp/`、warmRtp、Analyzer 国标 URL / 离线 / 自动 INVITE 已跑通；补《国标功能使用说明书》。真机与 P3 截图仍缺，phase 保持 3 |
| 2026-09-05 | 3 | 预览永久冻帧已修；P3 截图已交；模拟器偶发闪断可自愈；phase 仍为 3（等真机与 B/C 联调） |
