# 当前阶段

```yaml
phase: 1
```

协作 AI **先读本文件再写业务代码**。阶段禁令只写在这里。过验收后只改上面的 `phase`，不要去改角色手册。

升级方式：验收点 N 通过后，把 `phase` 改成 **N+1**（点 4 通过后保持 `4`），并在文末记一行。由同学 C 开 `docs/` PR，或当场让 AI 只改本文件。

---

## phase = 1（验收点 1 进行中）

**允许**

- 部署原版 easySVA，走通 RTSP 预览、原 YOLO 布控、告警截图
- 写架构文档、部署笔记、操作步骤
- 准备 H.264 测试视频
- 在 Windows 安装 Python / ultralytics（只做环境，不接入主流程）

**禁止**

- 实现 GB28181（开 SIP、同步国标设备、设备类型字段、国标列表页）
- 实现睡岗（YOLO-Pose 接入 Analyzer、睡岗告警类型、布控睡岗选项）
- 改原有 YOLO 推理逻辑、破坏原 RTSP 流程

---

## phase = 2（验收点 1 已过，做睡岗）

**允许**：睡岗原型、ONNX、接入 `server/`、告警类型与布控选项、告警页；继续修部署和文档。

**禁止**：GB28181 业务（SIP、ZLM 国标同步、设备 `device_type` / 国标列表）。可为睡岗做接口草稿，不要合入国标功能。

---

## phase = 3（验收点 2 已过，做国标）

**允许**：ZLM SIP 5060、国标注册/预览/离线、ZLM 同步接口、设备表与列表区分 RTSP/GB28181、Analyzer 取国标播放 URL。

**禁止**：为赶进度拆掉睡岗或原 YOLO；国标与睡岗大改不要塞进同一个 PR。

---

## phase = 4（验收点 3 已过，联调交付）

**允许**：两种设备源的睡岗联调、断线重连、回归原功能、手册 / 视频 / PPT。

**禁止**：无关重构、重写技术栈。

---

## 变更记录

| 日期 | phase | 说明 |
| --- | --- | --- |
| 2026-08-31 | 1 | 初始：验收点 1 |
